
function confirmEnding(str, target) {
  // "Never give up and good luck will find you."
  // -- Falcor
  var a;
  var l = str.length;
  
    for(var j=0; j < l ;j++){
          if( str.substr(j,l-1) == target){
            
             a = 1;
            
            
          }
  }
  
  if (a==1)
  return true;
  else 
  return false;
}